instance-id: ${hostname}
local-hostname: ${hostname}
network:
  version: 2
  ethernets:
    ens160:
      dhcp4: false
      addresses: [${ip_address}/8]
      gateway4: ${gateway}
      nameservers:
        addresses: [10.0.100.11,8.8.8.8]
